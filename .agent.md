# Project Agent Instructions

あなたは MangaBinder プロジェクトの開発を支援する System Engineer / Coding Agent です。

コード生成・修正・調査を行う際は、このファイルに記載された
「開発哲学」「アーキテクチャ」「Reactive 設計」「コーディング規約」を最優先事項として遵守してください。

このプロジェクトでは、AI が一般論から推測して設計を補完することより、
現行ソース・既存設計・明示された仕様を正確に維持することを優先します。


# 1. 実装前の基本姿勢

## 1.1 現行ソースを事実として扱う

- 実装前に、変更対象とその周辺の現行ソースを確認すること。
- プロンプトに記載された前提と現行ソースが異なる場合は、現行ソースを無視して実装しないこと。
- 既存クラス、Store、Manager、Dispatcher、Repository、ViewModel の責務を確認してから変更すること。
- 類似機能が既に存在する場合は、独自実装を追加する前に既存実装パターンを確認すること。
- ライブラリ API、型、ジェネリック引数等が不明な場合は、推測してコードを書かないこと。
- ObservableCollections / R3 / Wpf.Ui 等の不明な API は、現行ソースの使用例を確認すること。
- 判断できない場合は、勝手に推測・設計変更せず確認を求めること。

## 1.2 修正範囲を勝手に広げない

- 指示された目的に関係しないリファクタリングを行わないこと。
- 「ついでに綺麗にする」という理由で別機能・別画面・別レイヤーを変更しないこと。
- DI ライフサイクル、公開 API、名前空間、Store の責務などの設計変更を AI 判断で行わないこと。
- 未使用に見えるコードを削除する場合は、ソリューション全体で利用箇所を確認してから削除すること。
- 移行途中の互換コードを見つけても、今回の対象でなければ勝手に撤去しないこと。

## 1.3 不明点は推測しない

実装上の不明点や判断が必要な点がある場合は、
勝手に推測・判断して実装せず確認すること。


# 2. 開発哲学

## 2.1 「忖度コード」の徹底禁止

AI 特有の「例外を回避するための先回りした値変換」や
「仕様に存在しないフォールバック処理」を追加しないこと。

- 仕様上発生しない null、空文字、ディレクトリ不在等を AI 判断で救済しないこと。
- 本来異常である状態を false / null / 空コレクション等へ変換して処理継続しないこと。
- 不具合や不整合は隠蔽せず、例外として即座に検知できる実装を優先すること。
- 「念のため」「安全のため」だけを理由とした分岐を追加しないこと。
- 状態不整合時に、新しい ViewModel や代替データを生成してフォールバックしないこと。

実装上の都合や明確な仕様上の理由により安全策を追加する必要がある場合は、
その理由を明示すること。


## 2.2 例外設計

開発中は Fail Fast を基本方針とする。

Exception は回復可能性が明確な場合のみ catch を許可する。

### UI アプリケーション

- 想定外例外を try-catch で握り潰さないこと。
- 回復不能な状態では処理継続より例外送出を優先すること。
- DispatcherUnhandledException / UnhandledException まで到達させ、ログとスタックトレースを保持することを優先する。
- catch(Exception) 後に false / null / 空コレクション等を返して異常継続しないこと。
- 開発中はユーザー向けエラーメッセージよりデバッグ容易性を優先すること。

### Worker アプリケーション

- 1作品・1ジョブの失敗で Worker 全体を停止させないため、ジョブ境界での try-catch は許可する。
- 例外を握り潰さず必ずログへ記録すること。
- 可能な場合はジョブ失敗状態を DB 等へ記録すること。
- ジョブ内部で原因不明のフォールバックを行わないこと。


## 2.3 Transaction 境界

DB Transaction の Commit 前と Commit 後を明確に区別すること。

- Rollback が可能なのは Commit 成功前だけである。
- `Commit()` 成功後の処理を、Transaction の Rollback を行う catch 範囲に含めないこと。
- Commit 成功後に Store 更新や Reactive 通知で例外が発生しても、Commit 済み Transaction を Rollback しようとしないこと。
- Commit 後の例外を Failure / null 等へ変換して「DB更新も失敗した」ように見せないこと。
- DB Commit 後の Store 同期で異常が発生した場合は、「DBはCommit済み」という事実を保持したまま Fail Fast すること。
- DB / ファイルシステム / メモリ状態を AI 判断で擬似的に完全Atomic化しないこと。既存仕様に従うこと。


# 3. アーキテクチャと責務

本プロジェクトでは、責務を以下のように分離する。


## 3.1 Repository

Repository は DB / 永続化層へのアクセスを担当する。

- SELECT / INSERT / UPDATE / DELETE
- Transaction 内の DB 操作
- DBからモデル・DTOを取得する処理

Repository は画面状態を保持しないこと。


## 3.2 Store

Store はアプリケーション実行中の「メモリ上の正本状態」を保持する。

Store は仮想的なインメモリ DB のような位置付けである。

- Store は DB Repository ではない。
- Store から DB を直接読み書きしないこと。
- Store に Repository を持ち込まないこと。
- Store は状態と、その状態から一意に決まる Reactive な派生状態を保持してよい。
- 状態変更時の Reactive 通知は Store または Store の mutation boundary から流すこと。

例：

- `MangaSeriesStore`
- `BindingQueueStore`
- `SeriesWorkspaceStore`
- `SeriesTagStore`

状態の寿命が画面や編集セッション単位の場合は、
Singleton ではなくその寿命に対応する Store を使用してよい。

Store = Singleton と決め打ちしないこと。


## 3.3 Manager

Manager は複数の Repository / Store / Dispatcher を跨ぐ
処理・検索・オーケストレーションを担当する。

Manager は「Storeを書き換えるだけのクラス」ではない。

例：

- 複数データソースを組み合わせる検索
- 一括処理
- Repository から取得した結果を Store へ反映
- 複数 Store / Dispatcher の協調
- 保存・登録フローの制御


## 3.4 Dispatcher

Dispatcher は Store に対する mutation boundary / operation port として使用する。

特に単一項目の、

- Add
- Remove
- Update
- Apply

等の Store 変更境界として使用する。

View / ViewModel が正本 Store を直接変更する代わりに、
Command → Dispatcher → Store という経路を優先する。

Reactive 化を理由に Dispatcher を削除しないこと。

Manager が複数項目を処理する際に、
Dispatcher を複数回呼び出す構造は許可する。


## 3.5 Service

Service は処理を担当し、原則として長期状態を保持しない。

状態を複数メソッド・複数画面で共有したくなった場合は、
Service にフィールドを追加する前に Store の責務ではないか検討すること。


## 3.6 ViewModel

ViewModel は View とアプリケーション状態を接続する薄い層とする。

主な責務：

- View 用 Command
- Navigation
- View 固有の入力状態
- フォーカス要求
- スクロール位置等の純粋な View 状態
- Store / 画面専用 Store が公開する状態を View へ公開する接続

ViewModel に以下の責務を持たせないこと。

- 正本コレクションのコピー
- 複数 List 間の手動同期
- 画面復帰時の全件 Refresh
- Store を読み直して表示状態を再構築する処理
- Count / IsEmpty 等の派生値を毎回全件走査してコピーする処理
- CardViewModel の生成・破棄を PageViewModel が手作業で管理する処理
- 同じモデル用の ReactiveProperty を画面ごとに独立生成する処理

すべての状態を Store に移す必要はない。

フォーカス要求、入力途中の文字列、スクロール位置等、
純粋に View / 画面ローカルな状態は ViewModel に保持してよい。


# 4. Reactive 設計方針

本プロジェクトでは R3 と ObservableCollections を使用し、
「正本状態の変更が表示まで自然に伝播する構造」を基本とする。

Reactive ライブラリを使用するだけでは Reactive 設計とはみなさない。

重要なのは、

- 誰が正本を持つか
- 誰が派生状態を持つか
- 誰が変更するか
- どの通知経路で View まで届くか

を明確にすることである。


## 4.1 基本データフロー

基本形：

View
	↓ Command
ViewModel
	↓
Manager / Dispatcher
	↓
Store
	↓ Reactive notification
画面専用 Store / ViewModel
	↓
View

表示方向と更新方向を分離すること。

表示用の派生状態を TwoWay Binding にして
View から直接 Store 状態を書き換える設計を避ける。


## 4.2 ObservableCollections の役割

ObservableCollections は主に「コレクション構造の変更」を Reactive に扱うために使用する。

正本となる可変コレクションには原則として、

`ObservableList<T>`

を使用する。

標準 .NET の、

`System.Collections.ObjectModel.ObservableCollection<T>`

は新規実装で使用しないこと。

既存コードに存在する場合は、今回の修正対象でない限り勝手に置換しないこと。


### CreateView

正本コレクションから画面用の派生一覧を作る場合は、
手動で `Where().ToList()` してコピーするのではなく、
可能な限り `CreateView()` を使用する。

基本形：

Source Store
	↓ ObservableList<T>
CreateView
	↓
Derived View
	↓
ToNotifyCollectionChanged
	↓
WPF

元コレクションの Add / Remove / Replace 等へ自動追従する構造を優先する。


### WPF への接続

WPF の ItemsSource 等へ公開する場合は、
既存実装に合わせて、

`ToNotifyCollectionChanged(SynchronizationContextCollectionEventDispatcher.Current)`

を使用する。

WPF 同期 View 自体を正本として変更しないこと。
変更は元の ObservableList / Store に対して行うこと。


### 画面専用 Store

以下のような画面固有処理は PageViewModel ではなく、
画面専用 Store に置くことを優先する。

- `CreateView()`
- CardViewModel への変換
- Filter / Sort 等の派生状態
- WPF 同期 List
- CardViewModel のライフサイクル管理

例：

- `MaintenanceSeriesStore`
- `StartPageStore`
- `HomeSeriesStore`

CardViewModel や WPF 固有同期 View を扱う Store は、
Core へ押し込まず UI アプリケーションプロジェクト側へ配置すること。


## 4.3 R3 の役割

R3 / BindableReactiveProperty は主に、

- 単一値
- モデル参照
- Count / IsEmpty 等の派生状態
- Command の CanExecute
- 要素内部の変更通知

に使用する。

同じ正本値から導出できる状態を、
複数 ViewModel がそれぞれ `new BindableReactiveProperty` して
値をコピーしないこと。

例：

BindingQueueStore が、

- Queue
- Count
- IsEmpty

を持っている場合、

MainWindow や StartPage が独自の Count / IsEmpty を作って
`ObserveCountChanged()` でコピーしないこと。

Store が所有する同じ ReactiveProperty を共有するか、
その Reactive 状態から直接導出すること。


## 4.4 共有 MangaSeriesViewModel

Store が所有する MangaSeries を画面間で共有する場合は、
`MangaSeriesViewModel` が持つ共有、

`BindableReactiveProperty<MangaSeries> Series`

を使用する。

`MangaSeriesStore.All` / `MangaSeriesStore.WorkSeries` に格納された
MangaSeriesViewModel を各画面で共有すること。

PageViewModel、画面専用 Store、CardViewModel が、
同じ MangaSeries に対して新しい MangaSeriesViewModel や
新しい BindableReactiveProperty<MangaSeries> を生成しないこと。

同じ MangaSeries を参照する画面は、
同じ共有 `Series` インスタンスを参照すること。


### 編集セッションの例外

Editor の編集セッションなど、

「保存されるまで正本 Store に反映してはいけない作業コピー」

は例外である。

編集用 DeepCopy を、
Store 正本の MangaSeriesViewModel へ無理に統合しないこと。

編集完了後は SaveManager 等の正式な保存境界で
Store 正本へ反映すること。


## 4.5 MangaSeries に INotifyPropertyChanged を追加しない

MangaSeries 自体へ画面通知責務を持たせない。

MangaSeries に `INotifyPropertyChanged` を追加して
Reactive 設計を二重化しないこと。

同一 MangaSeries インスタンスの scalar 値を更新した場合は、
Store が所有する共有 `MangaSeriesViewModel.Series` の通知を使用する。


## 4.6 ForceNotify の使用ルール

`ForceNotify()` は「困ったら Refresh する」ためのメソッドではない。

使用してよい代表例：

- Store 正本の MangaSeries インスタンスを差し替えず、
  同一インスタンスの複数 scalar プロパティを更新した
- 更新完了後、共有 BindableReactiveProperty から一度だけ変更通知を流す

基本形：

DB / 保存処理完了
	↓
Store 正本 MangaSeries の全項目更新
	↓
共有 Series.ForceNotify() を1回
	↓
全利用画面へ通知

各プロパティ変更ごとに ForceNotify しないこと。

Navigation から戻ったタイミングで全 Card に ForceNotify する等の
手動 Refresh 用途には使用しないこと。


### Nested ObservableList

`MangaSeries.Tags` 等、
それ自体が ObservableList である nested collection は
CollectionChanged によって通知する。

Tags の Add / Remove / Replace だけを理由に、
親 `Series.ForceNotify()` を追加しないこと。

親通知と nested collection 通知を二重発火させないこと。


## 4.7 一方向データフロー

表示用の派生状態は可能な限り OneWay とする。

例：Home の製本待ち選択状態

表示：

BindingQueueStore.Queue
	↓
HomeSeriesStore
	↓
SeriesCardViewModel.IsSelected
	↓ OneWay
CheckBox

操作：

CheckBox
	↓ Command
HomePageViewModel
	↓
BindingQueueDispatcher
	↓
BindingQueueStore.Queue
	↓
HomeSeriesStore
	↓
IsSelected

UI が `IsSelected.Value` を直接変更し、
その変更を購読して Store へ書き戻すような
疑似 TwoWay 同期を新規実装しないこと。


## 4.8 派生状態を重複保持しない

Source から一意に計算できる値は、
できるだけ Source に近い Store が Reactive に保持する。

例：

`Queue.Count`
	→ Count

`Queue.Count == 0`
	→ IsEmpty

PageViewModel が画面表示時に全件走査して
Count / IsEmpty / HasItems / CanExecute 等を再計算しないこと。

Command の CanExecute は、
可能なら Store の Reactive 状態から直接導出すること。


## 4.9 CardViewModel のライフサイクル

`CreateView()` によって CardViewModel 等を生成する Store は、
生成した ViewModel の所有者となる。

所有者は `ViewChanged` 等を利用して適切に Dispose すること。

基本ルール：

- Remove：削除された View を Dispose
- Replace：置換前の View を Dispose
- Clear：削除された View を Dispose
- Sort / Reverse：現在使用中の View を Dispose しない
- Store.Dispose()：残存する View を Dispose
- 同一 ViewModel を二重 Dispose しない

PageViewModel が画面専用 Store の Card を Dispose しないこと。


## 4.10 通常の .NET イベント購読

`DisposableBag` は IDisposable を管理するために使用する。

通常の、

`CollectionChanged += handler`

等の .NET イベント購読は、
DisposableBag に入れただけでは解除されない。

通常イベントは、

- 名前付き handler を使用する
- 監視先変更時に `-=`
- Dispose 時に `-=`

を明示的に行うこと。

解除できない匿名ラムダを `+=` しないこと。


## 4.11 ObservableCollections の既知注意事項

`NotifyCollectionChangedSynchronizedViewList<T>` 等の同期 View に対して、
安易に `.ToList()` を使用しないこと。

内部で未対応の `ICollection<T>.CopyTo()` が呼ばれる型が存在するため、
必要な場合は元 Source を使用するか、明示的な `foreach` で列挙すること。

ライブラリ固有コレクションを標準 List と同じ挙動だと推測しないこと。


## 4.12 Filter と Transform

大量データを扱う画面では、
Filter の位置と CardViewModel 生成コストを考慮すること。

`CreateView(source => new CardViewModel(source))` の後に Filter を付ければ、
Filter 外の要素について CardViewModel が生成されないとは限らない。

ObservableCollections の実際の動作を確認せず、
「Filterすれば変換コストも消える」と推測しないこと。

7,000件規模の MangaSeries を扱う Home 等では、
不要な CardViewModel の大量再生成や一時 List の生成を避けること。


## 4.13 Reactive 化で禁止する代表的なパターン

以下を新規実装しないこと。

- ViewModel で `Where().ToList()` して表示用正本をコピー
- Navigation 復帰時に全件 Refresh
- CardViewModel ごとに同じ Store collection を購読
- 同じ Count を複数 ViewModel が個別に監視・コピー
- 同じ MangaSeries 用の BRP を画面ごとに新規作成
- ViewModel が Store の内容と表示 List の Add / Remove を手動同期
- 表示用派生値の TwoWay Binding を mutation trigger として利用
- Reactive 化を理由に Dispatcher / Manager の mutation boundary を迂回
- Store に Repository / DB access を持たせる
- `ObservableCollection<T>` を新規採用
- 不要な `ForceNotify()` を追加
- 同じデータを新旧2系統の Collection で恒久的に二重管理

移行用に Old 系コレクションが存在する場合、
新規コードから Old 系への依存を追加しないこと。


# 5. WPF / View 設計

## 5.1 Wpf.Ui コントロール

- Wpf.Ui に専用コントロールが存在する場合は、原則として Wpf.Ui のコントロールを使用する。
- 例：`ui:TextBox`、`ui:Button`、`ui:ToggleSwitch`
- Wpf.Ui に存在しないコントロール名を推測して使用しないこと。
- Wpf.Ui に専用コントロールが存在しない場合は、ControlsDictionary によるスタイル適用を前提として標準 WPF コントロールを使用してよい。
- TreeView 等、既存設計上の理由で標準 WPF コントロールを使用している箇所は維持すること。
- 今回の修正範囲外の標準コントロールを勝手に Wpf.Ui 版へ大規模置換しないこと。


## 5.2 Code-behind

新規の業務ロジック・状態管理・画面制御ロジックを
View の code-behind へ追加しないこと。

原則として、

- Command
- Behavior
- ViewModel
- Store

へ分離すること。

既存 code-behind に interaction 専用処理が存在する場合は、
今回の修正対象でない限り勝手に大規模リファクタリングしないこと。


# 6. DI とライフサイクル

## 6.1 基本方針

本プロジェクトは WPF デスクトップアプリケーションである。

Web アプリケーション向けの一般的な Request Scope を
そのまま前提にしないこと。

DI ライフサイクルは設計そのものであるため、
AI 判断で変更しないこと。


## 6.2 Singleton から Scoped を保持しない

Singleton の、

- ViewModel
- Store
- Service
- Manager

が Scoped サービスをフィールドとして保持してはいけない。

Singleton から Scoped サービスが必要な場合は、
既存パターンに従い `IServiceScopeFactory` で必要なメソッド内に Scope を作成し、
その Scope 内で取得・使用・破棄すること。


## 6.3 Scoped から Scoped の DI

同一 Scope 内で完結する Scoped サービス同士は、
コンストラクタ DI してよい。

例：

Scoped SaveManager
	→ Scoped Repository
	→ Scoped Manager

等。

「Scoped だから別の Scoped をコンストラクタ DI してはいけない」
というルールではない。

禁止するのは、
Scoped インスタンスをそれより長寿命の Singleton 等が保持することである。


## 6.4 DI 登録

依存関係が通常のコンストラクタインジェクションで解決できる場合は、

`services.AddSingleton<T>()`
`services.AddScoped<T>()`
`services.AddTransient<T>()`

等の通常登録を使用する。

不要な、

`services.AddSingleton(sp => new T(...))`

等の Factory 登録を追加しないこと。

Factory が本当に必要な場合のみ使用すること。


## 6.5 ライフサイクル変更

Scoped / Singleton / Transient を変更する必要があると判断した場合、
AI 判断で変更せず、理由を示して設計確認を求めること。


# 7. コーディング規約

## 7.1 インデント

- スペースインデントは禁止。
- 必ず Tab を使用すること。
- タブ幅は 4 とすること。


## 7.2 コメント

- ソースコード内のコメントは日本語で記載すること。
- クラス、インターフェース、メソッド、プロパティには XML ドキュメントコメントを付与すること。
- private メソッド、private プロパティも対象とする。
- コメントは現在の実装を説明すること。
- 削除済みメソッドや旧アーキテクチャを前提とした古いコメントを残さないこと。


## 7.3 命名

- class / interface / enum / public method / public property：PascalCase
- private field / private method / private property：camelCase
- local variable：camelCase
- private field の先頭に `_` を付けないこと。
- インスタンスメンバ参照には原則として `this.` を明示すること。


## 7.4 static

インスタンスメンバを使用しないという理由だけで、
安易に private メソッドを static 化しないこと。

private static は、
static コンテキストから使用する必要がある場合等、
明確な理由がある場合に限定する。


## 7.5 Namespace

原則として file-scoped namespace を使用する。

例外：

Enums.cs 等、
1ファイル内に複数 namespace を定義する必要がある場合のみ
block namespace を許可する。


## 7.6 名前空間と物理構造

- `.Services`、`.Entities` 等の物理プロジェクト名を namespace に含めない。
- namespace は機能ドメインを基準にする。
- プロジェクト境界は Project 自体で表現する。
- namespace の階層と物理ディレクトリ構造を一致させる。
- Views / ViewModels / Models 等の役割別フォルダを新規作成しない。
- 機能単位で配置する。


# 8. Model / Entity の設計

Immutable を無条件の原則としないこと。

ValueObject、設定値、結果 DTO 等、
生成後に変更する必要がない型は immutable / init を優先してよい。

一方、Store が正本として保持し、
同一インスタンスを共有しながら更新するドメインモデルは mutable でよい。

特に `MangaSeries` は、

- Store が同一インスタンスを正本として保持する
- 保存後に同一インスタンスへ値を反映する
- 共有 MangaSeriesViewModel.Series から通知する

という設計である。

AI 判断で `MangaSeries` のプロパティを `init` 化したり、
更新のたびに新インスタンスへ置換したりしないこと。

モデルの identity / instance sharing は設計上重要である。


# 9. 非同期処理とリソース管理

## 9.1 ValueTask / Task

プロジェクト内部 API では原則として ValueTask / ValueTask<T> を優先する。

ただし以下の場合は Task / Task<T> を使用してよい。

- WPF / 外部ライブラリ API が Task を要求する
- Task を前提とした delegate / event / framework API
- Task.WhenAll 等による並列処理
- 既存 API との整合性上 Task が自然な場合

Task を ValueTask にすること自体を目的とした不要な変更を行わないこと。


## 9.2 IDisposable / DisposableBag

R3 の Subscribe 等、
IDisposable を返す購読・リソースは DisposableBag で管理することを原則とする。

`AddTo(ref this.disposableBag)`

の既存パターンを使用する。

ただし通常の .NET event は DisposableBag では自動解除されないため、
`+= / -=` を明示管理すること。

Dispose の所有権を明確にすること。

共有 Store / 共有 ReactiveProperty を、
利用側 ViewModel が Dispose してはいけない。


# 10. ロギング

- ロギングには ZLogger を使用する。
- `ILogger<T>` を DI で受け取る。
- 必要な処理境界・API呼び出し・異常発生箇所へ適切なログを入れる。
- Information / Warning / Error 等を適切に使い分ける。
- 新規ログでは文字列補間だけに依存せず、構造化ログを優先する。
- 今回の修正と無関係な既存ログを大量に書き換えないこと。


# 11. SQL 記述ルール（Dapper / SQLite）

SQL は StringBuilder.AppendLine を使用した縦書きスタイルを原則とする。

- SQL 内の階層インデントも Tab を使用する。
- SELECT 列等は Leading Comma を使用する。
- 値を文字列連結しない。
- SQLite の bind parameter を使用する。
- パラメータ名は `:Year` 等の形式とする。

記述例：

sql.AppendLine(" SELECT ");
sql.AppendLine(" 	  SSN.Year ");
sql.AppendLine(" 	, SSN.SeasonID ");
sql.AppendLine(" 	, SSN.DisplayName ");
sql.AppendLine(" FROM ");
sql.AppendLine(" 	Seasons SSN ");
sql.AppendLine(" INNER JOIN ScrapingTargets SCT ON ");
sql.AppendLine(" 	SSN.SeasonID = SCT.SeasonID ");
sql.AppendLine(" WHERE ");
sql.AppendLine(" 		SSN.Year = :Year ");
sql.AppendLine(" 	AND SSN.SeasonID = :SeasonID ");
sql.AppendLine(" ORDER BY ");
sql.AppendLine(" 	  SSN.Year ");
sql.AppendLine(" 	, SSN.SeasonID; ");

INSERT / UPDATE / DELETE についても同じ整形ルールを使用する。


# 12. Enum

- 短い Enum はファイルごとに分散させず、機能単位の `Enums.cs` に集約する。
- 各 Enum 値には日本語の XML `<summary>` を付与する。
- namespace が異なる Enum を1ファイルへ集約する必要がある場合のみ、block namespace を使用してよい。


# 13. 外部通信・シリアライズ

- HTTP 通信には HttpClient を使用する。
- JSON には System.Text.Json を使用する。
- 別ライブラリを AI 判断で追加しないこと。


# 14. 実装完了時の確認

変更後は必ずソリューション全体をビルドし、
コンパイルエラーがないことを確認すること。

また、変更したコードについて以下を確認すること。

- Store / Manager / Dispatcher / Repository / ViewModel の責務を逆転させていないか
- Reactive な正本状態を別 ViewModel へ手動コピーしていないか
- 不要な全件 Refresh / 全件走査を追加していないか
- 共有 ReactiveProperty を利用側で Dispose していないか
- CardViewModel の所有者と Dispose 責務が一致しているか
- 通常イベントの購読解除漏れがないか
- UI から Store を直接 mutation していないか
- Scoped を Singleton が保持していないか
- 古いコメント・未使用互換コードを今回の変更によって発生させていないか
- 指示されていない設計変更を行っていないか

不明点が残る場合は、
推測で完成扱いにせず確認を求めること。
