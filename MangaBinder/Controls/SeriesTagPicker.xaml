<UserControl x:Class="MangaBinder.Controls.SeriesTagPicker"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MangaBinder.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- ToggleButton: タグ表示 & ポップアップ開閉 -->
        <ToggleButton
            x:Name="TagToggleButton"
            Padding="6,2"
            FontSize="11"
            Content="{Binding ButtonContent, RelativeSource={RelativeSource AncestorType=UserControl}}"
            Cursor="Hand"
            Background="{DynamicResource ControlFillColorDefaultBrush}"
            BorderThickness="0"
            Command="{Binding PreparePopupCommand}" />

        <!-- Popup: タグ選択チェックボックス一覧 -->
        <Popup
            x:Name="TagPopup"
            PlacementTarget="{Binding ElementName=TagToggleButton}"
            Placement="Top"
            IsOpen="{Binding IsChecked, ElementName=TagToggleButton, Mode=TwoWay}"
            StaysOpen="False"
            AllowsTransparency="True"
            PopupAnimation="Fade">
            <Border
                Background="{DynamicResource ApplicationBackgroundBrush}"
                BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Padding="8,6"
                MinWidth="280"
                MaxWidth="420">
                <ItemsControl
                    x:Name="TagItemsControl">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid
                                Columns="{Binding Tag, RelativeSource={RelativeSource AncestorType=ItemsControl}}" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <ContentControl>
                                <ContentControl.Style>
                                    <Style TargetType="ContentControl">
                                        <!-- デフォルト：実タグを表示 -->
                                        <Setter Property="Content" Value="{Binding}" />
                                        <Setter Property="ContentTemplate">
                                            <Setter.Value>
                                                <DataTemplate>
                                                    <local:SeriesTagItem DataContext="{Binding TagItem}" />
                                                </DataTemplate>
                                            </Setter.Value>
                                        </Setter>
                                        <!-- プレースホルダーの場合は空セル -->
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsPlaceholder}" Value="True">
                                                <Setter Property="ContentTemplate">
                                                    <Setter.Value>
                                                        <DataTemplate>
                                                            <Grid />
                                                        </DataTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ContentControl.Style>
                            </ContentControl>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>
        </Popup>
    </Grid>
</UserControl>
