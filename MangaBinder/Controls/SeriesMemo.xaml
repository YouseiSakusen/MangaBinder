<UserControl x:Class="MangaBinder.Controls.SeriesMemo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <!-- メモアイコン：HasMemo が true の場合のみ表示 -->
    <ui:SymbolIcon
        Symbol="Notepad20"
        Filled="True"
        Foreground="{DynamicResource OwnedCompletedBrush}"
        Visibility="{Binding HasMemo.Value, Converter={StaticResource BooleanToVisibilityConverter}}"
        FontSize="16">
        <!-- ToolTip: Memo 本文を表示 -->
        <ToolTipService.ToolTip>
            <TextBlock
                Text="{Binding Memo.Value}"
                TextWrapping="Wrap"
                MaxWidth="400" />
        </ToolTipService.ToolTip>
    </ui:SymbolIcon>
</UserControl>
