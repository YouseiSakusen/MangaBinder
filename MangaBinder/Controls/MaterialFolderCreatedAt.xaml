<UserControl x:Class="MangaBinder.Controls.MaterialFolderCreatedAt"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:local="clr-namespace:MangaBinder.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Visibility="{Binding HasCreatedAt.Value, Converter={StaticResource BooleanToVisibilityConverter}}">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
    </UserControl.Resources>

    <!-- 素材フォルダ作成日時をチップ状に表示 -->
    <Border
        BorderThickness="1"
        BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
        Background="{DynamicResource SubtleFillColorSecondaryBrush}"
        CornerRadius="4"
        Padding="5,2"
        ToolTip="素材フォルダ作成日時">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <!-- FolderOpenVertical16 アイコン -->
            <ui:SymbolIcon
                Symbol="FolderOpenVertical16"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                FontSize="14"
                Margin="0,0,4,0" />

            <!-- 素材フォルダ作成日時テキスト（yyyy/MM/dd HH:mm 形式） -->
            <TextBlock
                Text="{Binding CreatedAt.Value}"
                Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                FontSize="12"
                VerticalAlignment="Center" />
        </StackPanel>
    </Border>
</UserControl>
